import { Eventer } from "./Eventer";
export class Res extends Eventer {
  addResToExe(dirPath: string, exePath: string) {
    // return this.call(ClassId.Fs, FsMethodId.addResToExe, dirPath, exePath);
  }
}
