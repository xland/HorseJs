export enum ClassId {
  Sys,
  App,
  Window,
  Page,
}
export enum WindowMethodId {
  maximize,
  minimize,
  resize,
  move,
  regEvent,
  unregEvent,
  close,
  destory,
}
export enum WindowEventId {
  closing,
  sized,
}
