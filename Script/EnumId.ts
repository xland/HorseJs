export enum ClassId {
  Sys,
  Fs,
  App,
  Window,
  Page,
}
export enum FsMethodId {
  addResToExe,
}
export enum FsEventId {
  addResToExe,
}
export enum WindowMethodId {
  maximize,
  minimize,
  hide,
  show,
  restore,
  resize,
  move,
  close,
  destory,
  flash,
  setResizable,
  regEvent,
  unregEvent,
}
export enum WindowEventId {
  closing,
  sizePosChanged,
}
