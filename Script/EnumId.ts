export enum ClassId {
  Sys,
  Fs,
  App,
  Window,
  Page,
}
export enum FsMethodId {
  addResToExe,
}
export enum FsEventId {
  addResToExe,
}
export enum WindowMethodId {
  maximize,
  minimize,
  resize,
  move,
  regEvent,
  unregEvent,
  close,
  destory,
}
export enum WindowEventId {
  closing,
  sized,
}
