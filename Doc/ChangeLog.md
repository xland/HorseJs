- 最大化时，拖拽窗口有问题
- 最大化时，窗口覆盖了任务栏
- 设置了最大尺寸，就不应该允许窗口最大化，不然窗口最大化时调整尺寸会有问题。
- 启用/禁止窗口改变大小  在无边框窗口下有问题
- 允许通过自定义配置创建一个新窗口。



# todo:


- 读取本地文件
- 新建的窗口图标有问题
- 窗口间应该可以发送/接收消息
	- horse应该可以获得全部window对象	
- 把用户的UI文件加入到exe的资源
- 运行时读取UI文件资源
- 用这个程序开发一个工具，来构建最终exe
	- 指定UI路径
	- 指定图标ICON
	- 指定专用于安全的Token或Key
- 访问进程列表
- 启动第三方应用
- 访问剪切板
- 访问本地文件，增删改
- 创建托盘图标，托盘图标菜单
- 页内打开新窗口
- 为第三方页面注入JS脚本
- 如何禁止第三方页面访问本地API
- SQLite第三方库
